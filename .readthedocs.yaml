version: 2

formats: all

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - doc

build:
  os: ubuntu-22.04
  apt_packages:
    - python3-lgpio
  tools:
    python: "3"
